<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/add_contacts"
        android:icon="@drawable/ic_add_contacts"
        android:title="@string/add_contacts"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/direct_message"
        android:icon="@drawable/ic_direct_message"
        android:title="@string/direct_message"
        app:showAsAction="ifRoom"/>


    <!--<item android:id="@+id/direct_message"-->
        <!--android:title="@string/direct_message"-->
        <!--app:showAsAction="ifRoom"/>-->

</menu>